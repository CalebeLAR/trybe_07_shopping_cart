{
  "name": "shopping-cart",
  "version": "1.0.0",
  "description": "Protótipo de um carrinho de compras totalmente dinâmico que consome uma API do Mercado Livre para mostrar os produtos a venda",
  "main": "script.js",
  "scripts": {
    "test": "jest",
    "start": "live-server --open=index.html",
    "test:coverage": "jest --coverage",
    "lint": "eslint --no-inline-config --no-error-on-unmatched-pattern -c .eslintrc.json . --ext .js"
  },
  "dependencies": {
    "eslint-config-trybe-fundamentals": "1.1.0",
    "jest": "24.9.0",
    "live-server": "^1.2.2",
    "node-fetch": "^2.6.7"
  }
}
